SECRET_KEY=change-this-to-a-random-secret-value

# MySQL connection — defaults below match a local dev server with an empty
# root password (e.g. `brew install mysql`). Override for staging/production.
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=calacihouse
